# Ce répertoire est monté dans le container ClickHouse à /var/lib/clickhouse/user_scripts/
# Le service signoz-init-clickhouse y télécharge le binaire histogramQuantile au démarrage.
# Ce binaire est nécessaire pour les métriques de latence dans SigNoz.
