{
  "name": "taskforce-runner",
  "version": "0.1.0",
  "description": "Runner local de délégation TaskForce (ADR-013) : réclame les tâches déléguées à Claude Code, lance Claude Code sans interface dans un worktree git, ouvre la pull request et rend le résultat à TaskForce.",
  "type": "module",
  "private": true,
  "bin": {
    "taskforce-runner": "dist/index.js"
  },
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx src/index.ts",
    "check": "tsx src/index.ts --check",
    "once": "tsx src/index.ts --once",
    "test": "tsx --test test/*.test.ts"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
