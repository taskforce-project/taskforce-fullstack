; Le projet a un conflit de peer connu (vite@8 vs peer <=7 de @tailwindcss/vite).
; Il a été installé avec --legacy-peer-deps ; on le rend permanent pour que les
; installs (dont `shadcn add` qui lance `npm install`) n'échouent plus sur ERESOLVE.
legacy-peer-deps=true
