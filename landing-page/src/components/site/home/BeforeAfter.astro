---
/**
 * BeforeAfter (.astro) - « The problem » : la douleur ET sa démonstration dans une seule section
 * (fusion validée par le CEO le 24/09, home 14 → ~10 écrans). Anciennement deux blocs : `Problem`
 * (Platform.tsx, un titre + une litanie de 7 « Someone… ») puis ce Before/After. La litanie redisait
 * exactement ce que montre le diagramme « Before » : elle part, le titre et la chute restent.
 *
 * 2 pipelines React Flow sur la DA « traits intégrés » (`SectionShell` + `TraitGrid`). Le message
 * tient dans la LONGUEUR : 6 mains (le contexte fuit à chacune) contre 5 étapes gouvernées.
 */
import SectionShell from "@/components/site/sections/SectionShell.astro";
import TraitGrid from "@/components/site/sections/TraitGrid.astro";
import { StepChainFlow } from "@/components/site/flows/OrchestrationFlows";

const BEFORE = [
  { icon: "lightbulb", title: "Intent", sub: "lives in someone's head" },
  { icon: "users", title: "Meetings", sub: "notes scattered, half-remembered" },
  { icon: "file", title: "Specs", sub: "written once, never updated" },
  { icon: "ticket", title: "Tickets", sub: "the reason gets dropped" },
  { icon: "terminal", title: "Prompts", sub: "the agent is re-briefed from scratch" },
  { icon: "eye", title: "Review", sub: "the original context is already gone" },
];
const AFTER = [
  { icon: "flag", title: "Outcome" },
  { icon: "brain", title: "Context" },
  { icon: "calendar", title: "Plan" },
  { icon: "shield", title: "Approvals" },
  { icon: "rocket", title: "Ship" },
];
---

<SectionShell
  eyebrow="The problem"
  title="Teams don’t lose time building. They lose it transferring context."
  lead="Most software delivery moves the same way: an intention is passed from hand to hand until something ships. At each handoff the reason behind a decision gets thinner, and the reason is the part nobody writes down."
>
  <TraitGrid cols={2} class="mt-12">
    {/* Avant */}
    <div class="bg-card p-6 sm:p-8">
      <div class="mb-6 flex flex-wrap items-center justify-between gap-2">
        <span class="text-muted-foreground text-[12px] font-semibold tracking-[0.06em] uppercase">
          Before TaskForce
        </span>
        <span class="text-muted-foreground text-[12px]">6 handoffs · context leaks at each</span>
      </div>
      <div class="flow-canvas relative border" style={`height:${BEFORE.length * 90}px`}>
        <StepChainFlow steps={BEFORE} id="ba-before" w={260} client:only="react" />
      </div>
    </div>
    {/* Après */}
    <div class="bg-card p-6 sm:p-8">
      <div class="mb-6 flex flex-wrap items-center justify-between gap-2">
        <span class="text-primary text-[12px] font-semibold tracking-[0.06em] uppercase">
          With TaskForce
        </span>
        <span class="text-muted-foreground text-[12px]">One governed run · context kept</span>
      </div>
      <div class="flow-canvas relative border" style={`height:${AFTER.length * 90}px`}>
        <StepChainFlow steps={AFTER} id="ba-after" w={260} accentAll client:only="react" />
      </div>
    </div>
  </TraitGrid>

  {/* La chute de l'ancien bloc Problem : elle conclut le diagramme au lieu de le précéder. */}
  <div class="mt-10 flex flex-col gap-2 lg:flex-row lg:items-baseline lg:gap-8">
    <p class="text-foreground shrink-0 text-[18px] leading-8 font-semibold">
      The work isn’t the problem. The transfer is.
    </p>
    <p class="text-primary text-[15px] leading-7 font-medium">
      TaskForce keeps the context in one governed run, written down once and carried from outcome to
      ship.
    </p>
  </div>
</SectionShell>
